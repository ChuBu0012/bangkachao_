generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Info {
  id     Int    @id @default(autoincrement())
  name   String
  gender String
  age    Int
}
